version: 2
build:
  os: ubuntu-20.04
  tools:
    python: "3.10"

python:
  version: 3.10
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/conf.py

doc EOF

cat > docs/requirements.txt << 'DOCEOF'
sphinx>=5.0
sphinx-rtd-theme>=1.0
sphinx-autodoc-typehints>=1.0
